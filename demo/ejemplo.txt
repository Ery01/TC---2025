int cuadrado(int x) {
    return x * x;
}

void main() {
    int a;
    int b;
    int c = 2;
    int d;
    d = d; // Esta línea debería ser eliminada por el optimizador de asignaciones redundantes, si se implementa.
    d = d + 0; // Esta línea debería ser simplificada a d = d por el optimizador.
    d = d + 1;
    a = 5;
    b = 25;

    if (b > 10) {
        b = b - 2;
    } else {
        b = b + 2;
    }

    int sum_of_i_cubed = 0;
    int i = 0;
    // Bucle FOR: La actualización ahora es una asignación explícita (i = i + 1)
    for (i; i < 5; i = i + 1) {
        sum_of_i_cubed = sum_of_i_cubed + (i * i * i);
    }

    // Este bucle 'while' siempre sera infinito, y su 'i' interna es codigo muerto.
    while(true) {
        int i = 1;
    }
}
